# WakaRoute Web

## Product

WakaRoute（ワカルート）は、日本の高校受験に必要な力を細かく分解し、生徒ごとの理解状況、志望校、残り時間に合わせて学習ルートを提示するサービスです。

- 生徒本人と保護者の双方にとって、わかりやすく誠実な日本語を使用する。
- 実装されていない機能や未確認の成果を、利用可能または実証済みであるかのように表現しない。
- Web版をこのリポジトリで管理する。iOS版とAndroid版のコードは、それぞれ専用リポジトリが作成されるまで追加しない。

## Development task management

- すべての開発タスクは、AB ProjectsのプロジェクトID `89e7a1cf-f35e-4e65-a28d-39176ad06d39` で管理する。
- 実装を始める前に、対応するタスクを確認する。存在しない場合は、作業内容と完了条件が明確なタスクを作成する。
- 作業中はタスクのステータスを最新に保ち、重要な判断、ブロッカー、検証結果をタスクへ記録する。
- 必要な実装と検証が完了した場合にのみ、タスクを完了扱いにする。
- タスク完了時は、`ab_update_task` で進捗を100%にするだけで終えない。必ず `ab_complete_task` を使用し、ステータスをDone、進捗を100%、`isClosed=true`（現在のコネクター表示では `Complete: True`）にする。
- `ab_complete_task` の実行後は `ab_get_task` でタスクを再取得し、`Status: Done`、`Progress: 100%`、`Complete: True` になっていることを確認する。進捗が100%でも `Complete: False` のタスクは未完了として扱い、完了処理をやり直す。
- AB Projectsへ接続できない場合は、ローカル作業を安全に継続し、同期できなかった内容を作業報告に明記する。

## Technical conventions

- ASP.NET Core 10 MVCとRazor Viewsを使用する。
- MVCの責務を守り、Viewは表示、ControllerはHTTP処理、ビジネスロジックはServiceへ分離する。
- 既存のビジュアル言語、レスポンシブ設計、アクセシビリティを維持する。
- 新しい本番依存パッケージを追加する前に、その必要性を確認する。
- 秘密情報、接続文字列、APIキーをリポジトリへコミットしない。

## Visual design and motion

- カード、画像、ラベルを装飾目的だけで傾けない。方向、比較、状態変化など、傾き自体に意味がある場合だけ使用する。
- ページタイトルや重要な説明を画像へ焼き込まず、原則としてHTMLのテキストで表現する。
- 動きは、進捗、順序、関係、操作結果を理解しやすくする場合に使用する。常時動く装飾や、すべての要素への一律な出現演出は避ける。
- 操作できる説明図は、見た目だけでなくボタンなどの適切なHTML要素を使い、キーボード操作、フォーカス表示、状態通知に対応する。
- `prefers-reduced-motion`を尊重し、動きを止めても情報や操作が失われないようにする。

## Verification

- 変更後は少なくとも `dotnet build --configuration Release` を実行する。
- UIを変更した場合は、デスクトップとモバイルの両方で表示と主要操作を確認する。
- 警告やテスト失敗を無視したまま作業を完了しない。

## Git workflow

- ユーザーから明示的に依頼されない限り、commit、push、デプロイは行わない。
- ユーザーの既存変更を保持し、依頼と無関係な差分を変更しない。
